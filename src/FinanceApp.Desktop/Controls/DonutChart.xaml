<UserControl
    x:Class="FinanceApp.Desktop.Controls.DonutChart"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    SizeChanged="UserControl_SizeChanged">

    <Grid x:Name="ChartGrid" Height="220" Margin="0,10,0,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Donut Canvas -->
        <Canvas x:Name="DonutCanvas" Grid.Column="0" Width="180" Height="180" Background="Transparent" VerticalAlignment="Center" HorizontalAlignment="Center" />

        <!-- Legend Stack -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="10,0,0,0">
            <StackPanel x:Name="LegendStack" VerticalAlignment="Center" Spacing="8" />
        </ScrollViewer>
    </Grid>
</UserControl>
