<Page
    x:Class="FinanceApp.Desktop.Pages.ExpensesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Gastos"
                       FontSize="28" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="Nova Despesa"
                    Background="{StaticResource AccentVioletBrush}"
                    Foreground="White"
                    Padding="16,8"
                    CornerRadius="8"
                    Click="AddExpense_Click"
                    VerticalAlignment="Center"/>
        </Grid>

        <ProgressBar Grid.Row="1" IsIndeterminate="True"
                     Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                     Margin="0,8,0,8" />

        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.Expenses, Mode=OneWay}"
                  Background="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="12" Background="{StaticResource BgSurfaceBrush}" CornerRadius="8" Margin="0,4,0,4">
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Description}" FontSize="16" Foreground="{StaticResource TextPrimaryBrush}" />
                                <TextBlock Text="{Binding CompetenceDate}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding AmountExpected}" FontSize="16" Foreground="{StaticResource AccentVioletBrush}" VerticalAlignment="Center" />
                            <Border Grid.Column="2" Padding="8,4" CornerRadius="6" Background="{StaticResource BgElevatedBrush}" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Status}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                            </Border>
                            <Button Grid.Column="3" Background="Transparent" BorderThickness="0" Padding="4" VerticalAlignment="Center" Click="DeleteExpense_Click" Tag="{Binding Id}">
                                <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="#FF453A" />
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock Grid.Row="2" Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                   Foreground="{StaticResource AccentVioletBrush}"
                   VerticalAlignment="Bottom" />
    </Grid>
</Page>
