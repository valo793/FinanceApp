<Page
    x:Class="FinanceApp.Desktop.Pages.RecurringPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:FinanceApp.Desktop.Converters"
    xmlns:viewmodels="using:FinanceApp.Desktop.ViewModels">

    <Page.Resources>
        <converters:HexToBrushConverter x:Key="HexToBrushConverter" />
    </Page.Resources>

    <Grid Padding="24" Background="{StaticResource BgPrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Agenda e Planejamento"
                       FontSize="28" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                       
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button Content="Próximos 7 Dias" 
                        Click="WeekFilter_Click" 
                        Background="{StaticResource BgElevatedBrush}" 
                        Foreground="{StaticResource TextPrimaryBrush}"
                        Padding="16,8"
                        CornerRadius="6" />
                <Button Content="Próximos 30 Dias" 
                        Click="MonthFilter_Click" 
                        Background="{StaticResource BgElevatedBrush}" 
                        Foreground="{StaticResource TextPrimaryBrush}"
                        Padding="16,8"
                        CornerRadius="6" />
            </StackPanel>
        </Grid>

        <ProgressBar Grid.Row="1" IsIndeterminate="True"
                      Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                      Margin="0,0,0,8" />

        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.Occurrences, Mode=OneWay}"
                  Background="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:PlannedOccurrence">
                    <Border Padding="16" Background="{StaticResource BgSurfaceBrush}" CornerRadius="12" Margin="0,6,0,6">
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <!-- Indicador visual (Círculo de Categoria/Kind) -->
                            <Border Grid.Column="0" Width="12" Height="12" CornerRadius="6" 
                                    Background="{Binding TransactionKind, Converter={StaticResource HexToBrushConverter}}" 
                                    VerticalAlignment="Center" />
                            
                            <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Description}" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{Binding FrequencyText}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                                    <TextBlock Text="•" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                                    <TextBlock Text="Data Prevista:" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                                    <TextBlock Text="{Binding PlannedDateFormatted}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold" />
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,16,0" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding AmountFormatted}" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                                <TextBlock Text="Valor Previsto" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Right" />
                            </StackPanel>
                            
                            <Button Grid.Column="3" 
                                    Content="Pausar Recorrência" 
                                    Click="PauseTemplate_Click" 
                                    Tag="{Binding TemplateId}" 
                                    Background="Transparent" 
                                    Foreground="#FF453A" 
                                    BorderThickness="0" 
                                    VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <TextBlock Grid.Row="2" Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                   Foreground="{StaticResource AccentVioletBrush}"
                   VerticalAlignment="Bottom" />
    </Grid>
</Page>
