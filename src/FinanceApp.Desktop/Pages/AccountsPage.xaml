<Page
    x:Class="FinanceApp.Desktop.Pages.AccountsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Contas"
                       FontSize="28" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="Nova Conta"
                    Background="{StaticResource AccentCyanBrush}"
                    Foreground="Black"
                    Padding="16,8"
                    CornerRadius="8"
                    Click="AddAccount_Click"
                    VerticalAlignment="Center"/>
        </Grid>

        <ProgressBar Grid.Row="1" IsIndeterminate="True"
                     Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                     Margin="0,8,0,8" />

        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.Accounts, Mode=OneWay}"
                  Background="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="12" Background="{StaticResource BgSurfaceBrush}" CornerRadius="8" Margin="0,4,0,4">
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}" FontSize="16" Foreground="{StaticResource TextPrimaryBrush}" />
                                <TextBlock Text="{Binding AccountType}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding CurrentBalanceCached}" FontSize="16" Foreground="{StaticResource AccentCyanBrush}" VerticalAlignment="Center" />
                            <Border Grid.Column="2" Padding="8,4" CornerRadius="6" Background="{StaticResource BgElevatedBrush}" VerticalAlignment="Center" Margin="0,0,16,0">
                                <TextBlock Text="{Binding CurrencyCode}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                            </Border>
                            <ToggleSwitch Grid.Column="3" 
                                          IsOn="{Binding IsActive, Mode=OneWay}" 
                                          OnContent="Ativa" 
                                          OffContent="Inativa" 
                                          Toggled="AccountActive_Toggled" 
                                          Tag="{Binding}" 
                                          VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock Grid.Row="2" Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                   Foreground="{StaticResource AccentVioletBrush}"
                   VerticalAlignment="Bottom" />
    </Grid>
</Page>
