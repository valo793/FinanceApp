<Page
    x:Class="FinanceApp.Desktop.Pages.CategoriesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:FinanceApp.Desktop.Converters">

    <Page.Resources>
        <converters:HexToBrushConverter x:Key="HexToBrushConverter" />
    </Page.Resources>

    <Grid Padding="24" Background="{StaticResource BgPrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Categorias e Fontes de Receita"
                   FontSize="28" FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16" />

        <ProgressBar Grid.Row="1" IsIndeterminate="True"
                     Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                     Margin="0,0,0,8" />

        <Grid Grid.Row="2" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Column 0: Expense Categories -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Despesas" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                    <Button Grid.Column="1" Content="Nova Despesa" Background="{StaticResource AccentVioletBrush}" Foreground="White" CornerRadius="6" Click="NewExpenseCategory_Click" />
                </Grid>

                <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.ExpenseCategories, Mode=OneWay}" Background="Transparent">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12" Background="{StaticResource BgSurfaceBrush}" CornerRadius="8" Margin="0,4,0,4">
                                <Grid ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Width="12" Height="12" CornerRadius="6" Background="{Binding Color, Converter={StaticResource HexToBrushConverter}, ConverterParameter='AccentVioletBrush'}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="16" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2" Text="{Binding Icon}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
                                    <Button Grid.Column="3" Background="Transparent" BorderThickness="0" Padding="4" VerticalAlignment="Center" Click="DeleteExpenseCategory_Click" Tag="{Binding Id}">
                                        <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="#FF453A" />
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <!-- Column 1: Income Categories (Sources) -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Receitas (Fontes)" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                    <Button Grid.Column="1" Content="Nova Receita" Background="{StaticResource AccentCyanBrush}" Foreground="Black" CornerRadius="6" Click="NewIncomeCategory_Click" />
                </Grid>

                <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.IncomeCategories, Mode=OneWay}" Background="Transparent">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12" Background="{StaticResource BgSurfaceBrush}" CornerRadius="8" Margin="0,4,0,4">
                                <Grid ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Width="12" Height="12" CornerRadius="6" Background="{Binding Color, Converter={StaticResource HexToBrushConverter}, ConverterParameter='AccentCyanBrush'}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="16" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2" Text="{Binding Icon}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
                                    <Button Grid.Column="3" Background="Transparent" BorderThickness="0" Padding="4" VerticalAlignment="Center" Click="DeleteIncomeCategory_Click" Tag="{Binding Id}">
                                        <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="#FF453A" />
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</Page>
